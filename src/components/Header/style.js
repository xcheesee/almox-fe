const style = {
    header: { 
        color: (theme) => theme.palette.color.bg,
        minHeight: '3.25rem'
    },
    logo: { 
        fontSize: '1.5rem',
        '&:hover': { cursor: 'pointer' } 
    }
}

export default style;