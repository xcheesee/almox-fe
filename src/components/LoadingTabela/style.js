const style = {
    loading: { 
        background: (theme) => theme.palette.color.bgInterno, 
        minHeight: '56rem', 
        display: 'flex', 
        justifyContent: 'center', 
        alignItems: 'center' }
}

export default style;