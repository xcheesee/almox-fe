const style = {
    tableHead: { background: (theme) => theme.palette.primary.main},
    tableCell: { color: (theme) => theme.palette.color.main },
    boxTableCell: { 
        display: 'flex', 
        justifyContent: 'center', 
        alignItems: 'center',
        borderRadius: '5px'
    }
};

export default style;