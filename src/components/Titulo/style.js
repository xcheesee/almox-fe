const style = {
    titulo: { 
        fontSize: '1.75rem', 
        color: (theme) => theme.palette.color.bg,
        display: 'flex',
        gap: '0.75rem',
        alignItems: 'center'
    },
};

export default style;